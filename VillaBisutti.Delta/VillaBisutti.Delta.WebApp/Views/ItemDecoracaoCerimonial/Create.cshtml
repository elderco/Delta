@model IEnumerable<VillaBisutti.Delta.Core.Model.ItemDecoracaoCerimonial>
@{
	Layout = null;
	//ViewBag.Title = "Create";
}
@Scripts.Render("~/bundles/jqueryval")
<script type="text/javascript" language="javascript">
	var enviado;
	function Buscar(texto, item) {
		window.clearTimeout(enviado);
		if (texto == undefined)
			texto = "";
		if (item == undefined || isNaN(item) || item == "")
			item = 0;
		var url = "@Url.Action("Buscar", "ItemDecoracaoCerimonial", new { combo = "~0", texto = "~1" })";
		url = url.replace("~0", item).replace("~1", encodeURI(texto)).replace("&amp;", "&");
		enviado = window.setTimeout("LoadPage(\"" + url + "\", \"ResultView\");", 1000);

	}
	$(document).ready(function () {
		$("#EditarTipoItem").attr("disabled", "disabled");
		$("#ExcluirTipoItem").attr("disabled", "disabled");
		$("#TipoItemDecoracaoCerimonialId").change(function () {
			var id = $(this).val();
			if (id == "") {
				$("#EditarTipoItem").attr("disabled", "disabled");
				$("#ExcluirTipoItem").attr("disabled", "disabled");
			} else {
				$("#EditarTipoItem").removeAttr("disabled");
				$("#ExcluirTipoItem").removeAttr("disabled");
			}
			var urlEditar = "@(Url.Action("Edit", "TipoItemDecoracaoCerimonial"))/";
			var urlDeletar = "@(Url.Action("Delete", "TipoItemDecoracaoCerimonial"))/";
			$("#EditarTipoItem").click(function () {
				ShowPopUp(urlEditar + id, "Editar tipo de item de decoracao", 790);
			});
			$("#ExcluirTipoItem").click(function () {
				ShowPopUp(urlDeletar + id, "Excluir tipo de item de decoracao", 790);
			});
			Buscar($("#Nome").val(), $("#TipoItemDecoracaoCerimonialId").val());
		});
		$("#Nome").keyup(function () {
			if ($(this).val().length > 3)
				Buscar($("#Nome").val(), $("#TipoItemDecoracaoCerimonialId").val());
		});
		PreventNegativeNumbers("Quantidade");
		Buscar($("#Nome").val(), $("#TipoItemDecoracaoCerimonialId").val());
	});
</script>

@using (Html.BeginForm("ItemCreated", "ItemDecoracaoCerimonial"))
{
	@Html.AntiForgeryToken()

	<div class="form-horizontal row-fluid">
		<h4>Configuração de itens de Decoração</h4>
		<hr />
		@Html.ValidationSummary(true)

		<div class="form-group col-md-6">
			@Html.LabelFor(model => model.TipoItemDecoracaoCerimonial, new { @class = "control-label col-md-4" })
			<div class="col-md-2">
				@Html.DropDownList("TipoItemDecoracaoCerimonialId", (IEnumerable<SelectListItem>)ViewBag.TipoItemDecoracaoCerimonial, string.Empty, new { @class = "form-control" })
				@Html.ValidationMessageFor(model => model.TipoItemDecoracaoCerimonial)
			</div>
			<div class="col-md-6">
				<a href="@(Url.Action("Create", "TipoItemDecoracaoCerimonial"))" class="btn btn-sm btn-default PopUpActionLinks" id="CriarTipoItem"><span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Criar</a>
				<a href="javascript:void(0);" class="btn btn-sm btn-default" id="EditarTipoItem"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Editar</a>
				<a href="javascript:void(0);" class="btn btn-sm btn-default" id="ExcluirTipoItem"><span class="glyphicon glyphicon-trash" aria-hidden="true"></span> Excluir</a>
			</div>
		</div>

		<div class="form-group col-md-6">
			@Html.LabelFor(model => model.Nome, new { @class = "control-label col-md-4" })
			<div class="col-md-8">
				@Html.EditorFor(model => model.Nome)
				@Html.ValidationMessageFor(model => model.Nome)
			</div>
		</div>

		<div class="form-group col-md-6">
			@Html.LabelFor(model => model.Quantidade, new { @class = "control-label col-md-4" })
			<div class="col-md-8">
				@Html.EditorFor(model => model.Quantidade)
				@Html.ValidationMessageFor(model => model.Quantidade)
			</div>
		</div>

		<div class="form-group col-md-6">
			<div class="col-md-offset-4 col-md-8">
				<input type="submit" value="Salvar" class="btn btn-default form-control" />
			</div>
		</div>
	</div>
}
<div class="clear-fix"></div>
<div id="ResultView" class="row col-sm-12">

</div>
