@model VillaBisutti.Delta.Core.Model.RoteiroPadrao


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>RoteiroPadrao</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.Id)

		<h4>Não é possível alterar nenhum Roteiro Padrão delete e crie outro !!! </h4>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
				<a href="@(Url.Action("","RoteiroPadrao"))"class="btn btn-default" id="CriarTipoBebida"> Voltar</a>
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
